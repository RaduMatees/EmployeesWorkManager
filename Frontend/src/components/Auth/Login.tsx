import { Vue, Component } from 'vue-property-decorator'

@Component
export default class Login extends Vue {
  render() {
    return (
      <div class='container'>
        Login Page
      </div>
    )
  }
}
